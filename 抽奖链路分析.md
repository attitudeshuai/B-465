# 抽奖系统前后端数据链路 & 状态分叉分析

---

## 一、完整数据链路梳理

### 1. 管理员设置奖品（概率 & 库存）

| 环节 | 代码位置 | 数据流 |
|------|---------|--------|
| 前端管理员填写表单 | `PrizeManager.vue` → `handleSubmit()` | 表单数据 `formData`（含 `probability`、`stock`、`enabled` 等） |
| 调用 API | `api/index.js` → `createPrize()` / `updatePrize()` | HTTP POST/PUT `/api/prizes` 或 `/api/prizes/{id}` |
| Controller 接收 | `PrizeController.java` → `createPrize()` / `updatePrize()` | `PrizeDTO`（含 `probability` 千分比 1-1000、`stock`、`enabled`） |
| Service 处理 | `PrizeService.java` → `createPrize()` / `updatePrize()` | 将 DTO 映射为 `Prize` 实体，调用 `prizeRepository.save()` |
| 持久化 | `Prize.java` 实体 → `PrizeRepository` | 写入 `t_prize` 表，字段 `probability`、`stock`、`drawn_count`、`enabled` |

**关键数据模型**（`Prize.java`）：
- `probability`：千分比，1-1000（如 5 表示 0.5%）
- `stock`：库存数量
- `drawnCount`：已抽取数量
- `enabled`：是否启用

**管理员切换启用状态**：`PrizeController.java` → `toggleEnabled()` → `PrizeService.java` → `toggleEnabled()`，直接翻转 `enabled` 字段。

---

### 2. 前端加载奖品列表（转盘渲染）

| 环节 | 代码位置 | 数据流 |
|------|---------|--------|
| 页面初始化 | `Home.vue` → `onMounted()` → `loadPrizes()` | 调用 `getEnabledPrizes()` |
| API 请求 | `api/index.js` → `getEnabledPrizes()` | HTTP GET `/api/prizes/enabled` |
| Controller | `PrizeController.java` → `getEnabledPrizes()` | 调用 `PrizeService.getEnabledPrizes()` |
| Service 查询 | `PrizeService.java` → `getEnabledPrizes()` | `prizeRepository.findByEnabledTrueOrderBySortOrderAsc()` |
| Repository | `PrizeRepository.java` → `findByEnabledTrueOrderBySortOrderAsc()` | SQL: `WHERE enabled = true ORDER BY sort_order ASC` |
| 返回前端 | `PrizeDTO` 列表 | 包含所有 `enabled=true` 的奖品，**不区分库存是否为 0** |
| 转盘绘制 | `LotteryWheel.vue` → `drawWheel()` | 遍历 `props.prizes`，每个奖品占一个扇区 |
| 九宫格绘制 | `LotteryGrid.vue` → `gridItems` computed | 取前 8 个奖品填入 8 个格子（跳过中心按钮位） |

**⚠️ 核心问题**：前端转盘/九宫格渲染的是 `enabled=true` 的全量奖品列表，**不会过滤 `stock=0` 的奖品**。这意味着已无库存的奖品仍然会出现在转盘上。

---

### 3. 用户触发抽奖 → 后端执行算法

| 环节 | 代码位置 | 数据流 |
|------|---------|--------|
| 用户点击抽奖按钮 | `LotteryWheel.vue` → `startSpin()` / `LotteryGrid.vue` → `startDraw()` | emit `spin` / `draw` 事件 |
| 父组件处理 | `Home.vue` → `handleSpin()` | 调用 `drawLottery(userStore.user.id)` |
| API 请求 | `api/index.js` → `drawLottery()` | HTTP POST `/api/lottery/draw/{userId}` |
| Controller | `LotteryController.java` → `draw()` | 调用 `lotteryService.draw(userId, ipAddress)` |
| **Step 1** 验证用户 | `LotteryService.java` → `draw()` | 查用户、检查 `enabled`、检查 `remainingChances >= 1` |
| **Step 2** 获取展示列表 | `LotteryService.java` → `draw()` | `displayPrizes = prizeRepository.findByEnabledTrueOrderBySortOrderAsc()` |
| **Step 3** 筛选可用奖品 | `LotteryService.java` → `draw()` | `availablePrizes = displayPrizes.filter(p → p.stock > 0)` |
| **Step 4** 抽奖算法 | `LotteryService.java` → `drawPrize()` | 按 `probability` 权重随机选取，仅从 `availablePrizes` 中抽 |
| **Step 5** 扣减次数 | `UserRepository.java` → `decrementChances()` | `UPDATE ... SET remaining_chances = remaining_chances - 1 WHERE id = :userId AND remaining_chances > 0` |
| **Step 6** 扣减库存 | `PrizeRepository.java` → `decrementStock()` | `UPDATE ... SET stock = stock - 1, drawn_count = drawn_count + 1 WHERE id = :prizeId AND stock > 0` |
| **Step 7** 保存记录 | `LotteryRecord.java` | 写入 `t_lottery_record`，冗余记录 `prizeName` |
| **Step 8** 计算前端索引 | `LotteryService.java` → `draw()` | 在 `displayPrizes` 列表中查找中奖奖品的下标 → `prizeIndex` |
| 返回结果 | `DrawResultDTO` | 含 `prizeIndex`、`prizeName`、`prizeLevel`、`remainingChances` 等 |

---

### 4. 前端展示抽奖结果

| 环节 | 代码位置 | 数据流 |
|------|---------|--------|
| 接收后端响应 | `Home.vue` → `handleSpin()` | 从 `res.data` 获取 `DrawResultDTO` |
| 更新剩余次数 | `user.js` → `updateChances()` | 更新 Pinia store 和 localStorage |
| 转盘模式动画 | `LotteryWheel.vue` → `spinToIndex(prizeIndex, callback)` | CSS transform 旋转到 `prizeIndex` 对应扇区 |
| 九宫格模式动画 | `LotteryGrid.vue` → `runAnimation(targetPrizeIndex, callback)` | 跑马灯跳转，最终停在中奖格子 |
| 动画结束回调 | `Home.vue` → `showResult(result)` | 关闭 spinning 状态，弹出 `el-dialog` 展示中奖信息 |

**⚠️ 关键机制**：`DrawResultDTO.prizeIndex` 是后端在 `displayPrizes`（`enabled=true` 的列表）中计算的下标，前端用这个下标定位转盘扇区或九宫格格子。两端列表数据来源一致（同一个 API `/api/prizes/enabled`），所以**在列表未变化时**，索引对齐是正确的。

---

## 二、前端奖品渲染列表 vs 后端抽奖可用列表

| 维度 | 前端渲染列表 | 后端抽奖可用列表 |
|------|-------------|-----------------|
| 数据源 API | `GET /api/prizes/enabled` | `prizeRepository.findByEnabledTrueOrderBySortOrderAsc()` |
| 过滤条件 | `enabled = true` | `enabled = true AND stock > 0` |
| 代码位置 | `Home.vue` → `loadPrizes()` | `LotteryService.java` → `draw()` Step 3 |
| 是否包含库存为 0 的奖品 | ✅ 包含 | ❌ 不包含 |
| 是否包含 probability=0 的奖品 | ✅ 包含（如"再接再厉"） | ✅ 包含（但概率权重为 0，实际不可能被抽中） |
| 列表元素数量 | N 个（所有 enabled=true） | ≤ N 个（stock > 0 的子集） |

**差异影响**：

1. **转盘扇区数量** = 前端列表长度 N；后端可用列表长度 M ≤ N
2. `prizeIndex` 是基于前端列表（长度 N）计算的，只要中奖奖品同时存在于两个列表中，索引就是对的
3. 但如果中奖奖品**仅存在于前端列表而不在可用列表**中（即 stock=0 的奖品），它根本不会被抽到，所以不存在索引错位的问题——**问题在于转盘上展示了一个永远不可能被抽中的扇区**

---

## 三、三种状态分叉场景深度分析

### 场景 1：奖品库存归零

**时序描述**：

1. T0：前端加载奖品列表，奖品 A `stock=1`，转盘上显示 A
2. T1：用户甲点击抽奖，后端抽中 A，`decrementStock()` 执行成功，A 的 `stock` 变为 0
3. T2：用户乙的前端**尚未刷新**奖品列表，转盘上仍然显示 A
4. T3：用户乙点击抽奖

**后端行为**（`LotteryService.java` → `draw()`）：

- Step 2：`displayPrizes` 包含 A（A 的 `enabled=true`）
- Step 3：`availablePrizes = displayPrizes.filter(stock > 0)`，**A 被过滤掉**
- Step 4：`drawPrize(availablePrizes)` 只从有库存的奖品中抽取
- 结果：用户乙不可能抽中 A，转盘动画指向的奖品与实际结果**不矛盾**（因为后端返回的 `prizeIndex` 是基于 `displayPrizes` 计算的）

**但问题在于**：

- 前端转盘上的 A 扇区仍然可见，用户心理预期"可能抽中 A"
- 如果乙抽中了奖品 B，`DrawResultDTO.prizeIndex` 指向 `displayPrizes` 中 B 的下标，转盘动画转到 B——这在技术上是对的
- **但**：如果**所有奖品库存都为 0**，`availablePrizes` 为空，后端抛出 `BusinessException("所有奖品已抽完")`，前端收到错误，转盘停止——用户看到转盘上明明有奖品却提示"已抽完"
- **更隐蔽的问题**：`decrementStock()` 返回 `int`，如果返回 0（库存已为 0），`LotteryService` **没有检查返回值**（`LotteryService.java` 第 82-84 行），只在 `stock > 0` 时才调用 `decrementStock`，但**在并发情况下**，两个线程可能同时通过 `stock > 0` 检查

**数据流分叉图**：

```
前端状态          后端 DB 状态
┌──────────┐     ┌──────────┐
│ A stock=1│     │ A stock=0│  ← 已被其他用户抽走
│ B stock=5│     │ B stock=5│
└──────────┘     └──────────┘
   ↓                 ↓
 转盘显示 A      抽奖池不含 A
 转盘显示 B      抽奖池包含 B
   ↓                 ↓
 用户点击抽奖    → 后端只从 {B} 中抽 → 返回 B 的 prizeIndex
   ↓
 转盘动画到 B   ← 索引对齐，无视觉错误
 但用户预期可能抽中 A ← 心理预期偏差
```

---

### 场景 2：并发请求同时抽奖

**时序描述**：

1. T0：奖品 A `stock=1`
2. T1：用户甲和乙**几乎同时**点击抽奖，两个请求并发到达后端
3. T2：两个线程同时执行 `LotteryService.draw()`

**并发竞态分析**：

`LotteryService.draw()` 标注了 `@Transactional`，但**默认隔离级别下**：

- **Step 3 读操作**：两个事务都读到 `stock=1`，都把 A 加入 `availablePrizes`
- **Step 4 抽奖**：两个线程都可能抽中 A
- **Step 6 扣减库存**：`decrementStock()` 使用了 `WHERE stock > 0` 条件，第二个执行的事务会因 `stock` 已为 0 而返回 `affected rows = 0`

**具体分叉路径**：

| 情况 | 线程甲 | 线程乙 | 结果 |
|------|--------|--------|------|
| 甲先提交，乙后提交 | 抽中 A，`decrementStock` 返回 1 | 抽中 A，`decrementStock` 返回 0 | **超卖 Bug**：甲正常，乙的 `LotteryRecord` 记录了抽中 A，但库存实际未扣减成功，A 的 `drawnCount` 也不正确 |
| 甲乙都抽中其他奖品 | 不涉及 A | 不涉及 A | 无问题 |

**`decrementStock` 返回值被忽略**（`LotteryService.java` 第 82-84 行）：

```java
if (prize.getStock() > 0) {
    prizeRepository.decrementStock(prize.getId());
}
```

这里只检查了 `prize.getStock()`（此时还是事务开始时读到的旧值），而**没有检查 `decrementStock` 的返回值**是否为 1。如果返回 0，说明库存已被其他事务扣减完毕，但当前事务**没有回滚或抛异常**，继续保存了 `LotteryRecord`——**记录与库存不一致**。

**用户侧表现**：

- 甲的前端：转盘正常动画，显示抽中 A ✅
- 乙的前端：转盘也正常动画，显示抽中 A ❌（但 A 实际只有 1 份库存）
- 后果：两个用户都以为抽中了 A，但只有一份库存

---

### 场景 3：管理员中途修改概率

**时序描述**：

1. T0：前端加载奖品列表，奖品 A `probability=100`（10%），奖品 B `probability=900`（90%）
2. T1：用户点击抽奖，请求发往后端
3. T1.5（极小概率）：**在用户请求到达后端之前**，管理员修改了 A 的 `probability` 为 900（90%）
4. T2：后端执行 `draw()`，读取到最新的 `probability`

**分叉路径 A——后端已读到新概率**：

- 后端用新概率（A=900, B=900）执行抽奖算法
- 用户大概率抽中 A
- 前端转盘上 A 的扇区大小仍是旧比例（10%），但动画转到了 A
- **视觉上**：A 占转盘很小扇区，但指针停在了 A——用户可能觉得"概率不对"

**分叉路径 B——用户请求比管理员操作更早到达后端**：

- 后端用旧概率（A=100, B=900）执行抽奖
- 转盘动画与结果一致，无问题

**更严重的情况——管理员禁用奖品**：

1. T0：前端加载列表，A、B、C 三个奖品
2. T1：管理员禁用 A（`toggleEnabled`），A 的 `enabled=false`
3. T2：用户点击抽奖（前端仍显示 A、B、C 三个扇区）
4. 后端 `displayPrizes` = {B, C}，`availablePrizes` = {B, C}
5. 抽中奖品后，`prizeIndex` 在 {B, C} 组成的列表中计算
6. **但前端转盘是按 {A, B, C} 渲染的**，`prizeIndex=0` 对应的是 B 而非 A
7. 转盘动画指向错误的位置 🐛

**这是最严重的数据分叉场景**，原因：

- 前端缓存了 `enabled=true` 的旧列表
- 后端抽奖时用的是 `enabled=true` 的新列表
- 两个列表**元素不同**，`prizeIndex` 的语义就完全不同了

**分叉图**：

```
前端缓存列表（T0 时刻加载）     后端实时列表（T2 时刻查询）
┌─────────────────────┐      ┌─────────────────────┐
│ [0] A (已被禁用)     │      │ [0] B               │
│ [1] B               │      │ [1] C               │
│ [2] C               │      └─────────────────────┘
└─────────────────────┘
        prizeIndex=0 → A           prizeIndex=0 → B
                                     ↑
                              后端返回 prizeIndex=0
                              前端动画转到 A  ← 错误！
```

---

## 四、问题汇总表

| # | 场景 | 根因 | 严重程度 | 前端表现 | 后端数据 |
|---|------|------|---------|---------|---------|
| 1 | 库存归零，转盘仍显示 | 前端不过滤 `stock=0` 的奖品 | 中 | 转盘显示不可中奖品 | 抽奖池不包含该奖品 |
| 2 | 库存归零，全部抽完 | `availablePrizes` 为空时抛异常 | 中 | 转盘有奖品但提示"已抽完" | 所有奖品 stock=0 |
| 3 | 并发超卖 | `decrementStock` 返回值未检查 | 高 | 两用户都显示抽中同一奖品 | LotteryRecord 与 stock 不一致 |
| 4 | 管理员改概率 | 前端未实时刷新概率 | 低 | 转盘扇区比例与实际概率不符 | 使用最新概率抽奖 |
| 5 | 管理员禁用奖品 | 前端缓存旧列表，`prizeIndex` 错位 | **极高** | 转盘指向错误奖品 | 返回的 `prizeIndex` 基于新列表 |

---

## 五、改进建议

### 1. 修复并发超卖（优先级：高）

`LotteryService.java` → `draw()` 中，`decrementStock()` 的返回值必须检查：

- 若返回 0，说明库存已被并发扣减，应抛出 `BusinessException` 或重试抽奖
- 更优方案：使用 `SELECT ... FOR UPDATE` 悲观锁，或 `@Version` 乐观锁，确保同一奖品的库存扣减串行化

### 2. 修复 prizeIndex 错位（优先级：极高）

`DrawResultDTO.prizeIndex` 的正确性**完全依赖前端奖品列表与后端 `displayPrizes` 一致**。一旦管理员在抽奖期间增删或禁用奖品，索引就会错位。

方案：
- 后端在 `DrawResultDTO` 中直接返回 `prizeId` 和完整奖品信息，**不再依赖 `prizeIndex`**
- 前端在动画结束后，用 `prizeId` 匹配本地列表中的下标来定位
- 或在 `draw()` API 中同时返回当前后端的奖品列表快照，前端用快照做动画

### 3. 前端转盘过滤无库存奖品（优先级：中）

`Home.vue` → `loadPrizes()` 加载后，在传递给 `LotteryWheel` / `LotteryGrid` 之前，过滤掉 `stock <= 0` 的奖品，或在转盘上标记"已抽完"的灰色样式。

### 4. 前端定时刷新奖品列表（优先级：中）

在 `Home.vue` 中增加轮询机制（如每 30 秒 `loadPrizes()` 一次），缩小前端缓存与后端实际状态的窗口期。

### 5. 管理员操作时通知前端（优先级：低）

通过 WebSocket / SSE 推送奖品变更事件，前端收到后立即刷新转盘，彻底消除列表不一致的窗口。
