# 抽奖系统链路分析报告

## 勘误说明

**第一轮分析结论修正**：

原结论"库存归零最终一致"在**技术层面**是准确的——如果只是 `stock` 字段变化，列表结构（元素、顺序）不变，`prizeIndex` 确实正确。

但**实际生产场景中**这个结论存在误导性：
- 奖品库存归零后，管理员很可能会**禁用该奖品**（`enabled=false`）
- 一旦禁用，`displayPrizes` 列表元素发生变化
- 此时前后端列表不一致，`prizeIndex` 就会错位
- 最终导致"显示对不上"

**修正后的准确结论**：
- ✅ 单纯库存归零（`enabled` 仍为 true）：指针正确，显示一致
- ❌ 库存归零 + 禁用奖品（`enabled=false`）：指针错位，显示对不上！

---

## 一、系统架构总览

本系统采用前后端分离架构：
- **后端**：Spring Boot + JPA + MySQL
- **前端**：Vue 3 + Element Plus + Canvas（转盘/九宫格）

---

## 二、完整数据流向链路

### 2.1 管理员设置奖品（管理端链路

```
管理员操作 → 前端PrizeManager.vue → API层 → 后端Controller → 数据库
```

| 环节 | 文件/方法 | 数据内容 |
|------|----------|----------|
| 管理员登录 | `PrizeManager.vue | 登录验证 |
| 加载奖品列表 | `PrizeManager.vue:loadPrizes()` → `getAllPrizes()` | 调用 `PrizeController.getAllPrizes()` → `PrizeService.getAllPrizes()` → `PrizeRepository.findAll()` | 所有奖品完整信息（含库存、概率、状态） |
| 创建/编辑奖品 | `PrizeManager.vue:handleSubmit()` → `createPrize()` / `updatePrize()` | 调用 `PrizeController.createPrize()` / `updatePrize()` → `PrizeService.createPrize()` / `updatePrize()` | 奖品名称、概率、库存、等级、颜色、状态 |
| 切换启用状态 | `PrizeManager.vue:handleToggle()` → `togglePrize()` | 调用 `PrizeController.toggleEnabled()` → `PrizeService.toggleEnabled()` | 奖品 enabled 字段 |

**关键数据字段**（Prize.java
- `probability`: 千分比概率（1-1000）
- `stock`: 库存数量
- `enabled`: 是否启用
- `sortOrder`: 排序顺序

---

### 2.2 用户抽奖链路（前端展示）

```
页面加载 → 获取启用奖品 → 渲染转盘 → 用户点击抽奖 → 后端执行抽奖 → 动画展示 → 结果弹窗
```

| 环节 | 文件/方法 | 数据流向 |
|------|----------|----------|
| 页面初始化 | `Home.vue:onMounted()` → `loadPrizes()` | 调用 `getEnabledPrizes()` API → `PrizeController.getEnabledPrizes()` → `PrizeService.getEnabledPrizes()` → `PrizeRepository.findByEnabledTrueOrderBySortOrderAsc()` |
| 前端渲染奖品 | `Home.vue` → `LotteryWheel.vue:drawWheel()` / LotteryGrid.vue` | 根据 `enabled=true` 的奖品列表绘制转盘扇区/九宫格格子 |
| 用户点击抽奖 | `Home.vue:handleSpin()` | 前端校验登录状态、剩余次数 |
| 发起抽奖请求 | `drawLottery(userId)` → `LotteryController.draw()` | 传入用户ID |
| 后端执行抽奖 | `LotteryService.draw()` | 见下节详述 |
| 前端动画 | `LotteryWheel.vue:spinToIndex()` / `LotteryGrid.vue:runAnimation()` | 根据返回的 `prizeIndex` 执行动画 |
| 结果展示 | `Home.vue:showResult()` | 弹出中奖/未中奖弹窗 |

---

### 2.3 后端抽奖核心逻辑

**`LotteryService.draw()` 执行流程：

```
1. 验证用户状态和剩余次数
   ↓
2. 获取展示列表：查询 displayPrizes = （与前端完全一致的列表
   （PrizeRepository.findByEnabledTrueOrderBySortOrderAsc()）
   ↓
3. 可用列表：筛选 availablePrizes = displayPrizes 中 stock > 0 的奖品
   ↓
4. 抽奖算法：drawPrize(availablePrizes) - 基于概率加权随机
   ↓
5. 扣减用户次数：UserRepository.decrementChances()
   ↓
6. 扣减奖品库存：PrizeRepository.decrementStock()
   ↓
7. 保存抽奖记录：LotteryRecordRepository.save()
   ↓
8. 计算 prizeIndex：在 displayPrizes 中查找中奖奖品的位置
   ↓
9. 返回结果：DrawResultDTO（含 prizeIndex）
```

**关键方法**：
- `LotteryService.drawPrize()`: 概率加权随机算法
- `PrizeRepository.decrementStock()`: 原子化库存扣减（`UPDATE ... SET stock = stock - 1 WHERE stock > 0`

---

## 三、前后端奖品列表差异分析

### 3.1 两个核心列表的定义

| 列表类型 | 前端展示列表（displayPrizes） | 后端可用列表（availablePrizes） |
|----------|--------------------------|--------------------------------|
| **查询条件 | `enabled = true` | `enabled = true` **AND** `stock > 0` |
| **获取时机** | 页面加载时一次性获取 | 每次抽奖时实时计算 |
| **用途** | 绘制转盘扇区、确定 prizeIndex | 实际抽奖候选池 |
| **数据时效性** | 可能过期（页面打开后奖品状态变化不感知） | 实时（每次抽奖时查询DB） |

### 3.2 差异产生的根本原因

**前端展示列表与后端抽奖可用列表的不一致是问题的核心：**

1. **时间差问题**：前端在 `Home.vue:loadPrizes()` 只在 `onMounted()` 时调用一次，之后奖品状态（库存、enabled）变化时，前端不感知
2. **筛选条件差**：前端只过滤 `enabled=true`，后端抽奖时额外过滤 `stock>0`
3. **索引计算依赖展示列表**：`prizeIndex` 基于 `displayPrizes` 计算，确保动画定位准确，但抽奖只发生在**可见**奖品上

---

## 四、三种异常场景深度分析

### 4.1 场景一：奖品库存归零

**时序分析**：

```
T0: 用户A打开页面 → 前端加载奖品 [P1(stock=1), P2, P3] → 转盘显示3个奖品
T1: 用户B抽中P1 → 后端P1.stock = 0（但 enabled 仍为 true）
T2: 用户A点击抽奖（此时转盘仍显示P1）
    ↓
T2.1: 后端获取 displayPrizes = [P1, P2, P3]（enabled都为true，与前端一致）
T2.2: 后端筛选 availablePrizes = [P2, P3]（P1.stock=0 被排除）
T2.3: 抽奖算法只在 [P2, P3] 中选择
T2.4: 假设抽中P2，计算 prizeIndex = 1（在 displayPrizes 中的位置）
T2.5: 返回 prizeIndex=1，前端指针指向P2
```

**关键验证**：
- `prizeIndex` 基于 `displayPrizes`（过滤前的全量列表）计算，**不是** `availablePrizes`
- 代码注释明确说明：**"使用同一个 displayPrizes 列表"**（LotteryService.java:100）

**前后端状态分叉点**：
- **前端状态**：认为有3个奖品可抽（含P1）
- **后端状态**：实际只有2个奖品可抽（P1已无库存）
- **结果一致性**：✅ **指针指向正确！** 因为前后端用的是**同一个 displayPrizes 列表**计算索引

**潜在问题**：
- 用户心理预期："我看到P1在转盘上，为什么抽不到？"
- 前端奖品卡片显示P1库存为0时样式变灰，但转盘上P1扇区仍正常显示
- ✅ **这个场景不会导致"显示对不上"问题！**

---

### 4.2 场景二：并发请求同时抽奖

**时序分析**（P1.stock = 1，两个用户同时请求）：

```
T0: 用户A和用户B同时点击抽奖
    ↓
T1-A: LotteryService.draw() 开始（事务A）
T1-B: LotteryService.draw() 开始（事务B）
    ↓
T2-A: 事务A查询 displayPrizes = [P1, P2, P3]
T2-B: 事务B查询 displayPrizes = [P1, P2, P3]
    ↓
T3-A: 事务A筛选 availablePrizes = [P1, P2, P3]（P1.stock=1 > 0）
T3-B: 事务B筛选 availablePrizes = [P1, P2, P3]（P1.stock=1 > 0）
    ↓
T4-A: 事务A抽奖算法 → 抽中P1
T4-B: 事务B抽奖算法 → 抽中P1
    ↓
T5-A: 事务A执行 PrizeRepository.decrementStock(P1.id)
      → UPDATE ... WHERE stock > 0 → 成功（影响行数=1），stock=0
T5-B: 事务B执行 PrizeRepository.decrementStock(P1.id)
      → UPDATE ... WHERE stock > 0 → 失败（影响行数=0），stock仍=0
    ↓
T6-A: 事务A保存记录，返回成功
T6-B: 事务B保存记录，但库存扣减失败？
```

**关键发现**：代码中 `LotteryService.draw()` 第82-84行：
```java
if (prize.getStock() > 0) {
    prizeRepository.decrementStock(prize.getId());
}
```

**问题**：没有检查 `decrementStock()` 的返回值！如果返回0表示扣减失败，但代码继续执行，导致：
- 抽奖记录显示用户抽中了P1
- 但实际库存没扣减（超发）
- 或者更糟：`drawnCount` 也没增加

---

### 4.3 场景三：管理员中途修改概率/库存

**子场景A：修改概率**

```
T0: 用户打开页面 → 前端加载奖品 [P1(prob=100), P2(prob=200)]
T1: 管理员将 P1.prob 改为 500（通过 PrizeManager.vue）
T2: 用户点击抽奖
    ↓
T2.1: 后端使用最新概率计算（从DB查询）
T2.2: 抽奖结果按新概率分布
T2.3: prizeIndex 不变（排序没变）
```

**状态分叉**：
- 前端：不知道概率变化，显示的概率说明（如果有）是旧的
- 后端：使用新概率进行抽奖
- 影响：用户感知不到，抽奖公平性质疑

**子场景B：修改奖品排序/增删奖品/切换启用状态（真正导致"显示对不上"的场景！）**

```
T0: 用户打开页面 → 前端加载列表 [P1, P2, P3]
    前端转盘渲染：P1(index=0), P2(index=1), P3(index=2) ← 列表A
      
T1: 管理员操作（三种操作之一都会触发bug）：
   - 删除P2，添加P4 → 后端列表变为 [P1, P3, P4]
   - 或：修改P2和P3的排序 → 后端列表变为 [P1, P3, P2]
   - 或：禁用P2 → 后端列表变为 [P1, P3]
    后端 displayPrizes = 列表B（新列表）
      
T2: 用户点击抽奖
    ↓
T2.1: 后端 displayPrizes = 列表B（新的，与前端不一致！）
T2.2: 抽奖 → 抽中P3
T2.3: 计算 prizeIndex = 1（在列表B中的位置）
T2.4: 返回 prizeIndex=1 给前端
T2.5: 前端转盘仍按列表A渲染 → index=1 指向 P2！
```

**严重问题**：✅ **"显示对不上"真实复现！**

| 维度 | 前端转盘 | 后端计算 |
|------|---------|---------|
| 使用的列表 | 列表A：[P1, P2, P3] | 列表B：[P1, P3, P4] |
| index=1 指向 | ❗ P2 | P3 |
| 实际抽中 | - | P3 |
| 指针最终指向 | ❗ P2 | - |

**结论**：用户看到指针指向 P2，但弹窗显示抽中 P3！显示完全对不上！

---

## 五、核心问题总结

### 5.1 已发现的Bug

| Bug编号 | 问题描述 | 位置 | 严重程度 |
|--------|----------|------|----------|
| BUG-001 | 库存扣减失败未检查返回值，可能超发 | `LotteryService.draw()` L82-84 | 高 |
| BUG-002 | 奖品列表变化（增删、排序）导致 prizeIndex 错位 | 前后端数据不一致 | 极高 |
| BUG-003 | 前端奖品卡片显示库存状态，但转盘扇区不反映库存状态 | `Home.vue` / `LotteryWheel.vue` | 中 |

### 5.2 设计问题根源

**问题的本质**：前端渲染转盘时的列表与后端计算 prizeIndex 时的列表存在**时间差**，而 `prizeIndex` 的计算依赖于**抽奖时**的后端列表，而非**渲染时**的前端列表。

**关键澄清**：
- 不是「可用列表 vs 展示列表」的问题（这个已经处理好了）
- 而是「T0时刻的前端列表 vs T2时刻的后端列表」的问题（时间差导致的不一致）

当这两个列表不一致时：

| 操作类型 | 列表结构是否变化 | prizeIndex 是否错位 | "显示对不上"是否复现 |
|---------|----------------|-------------------|---------------------|
| 修改库存（stock↓） | ❌ 不变（元素和顺序都不变） | ✅ 正确 | ❌ 不复现 |
| 修改概率（probability） | ❌ 不变 | ✅ 正确 | ❌ 不复现 |
| 修改名称/颜色 | ❌ 不变 | ✅ 正确 | ❌ 不复现 |
| **增删奖品** | ✅ 变化（元素变了） | ❌ 错位 | ✅ **复现！** |
| **修改排序** | ✅ 变化（顺序变了） | ❌ 错位 | ✅ **复现！** |
| **切换enabled** | ✅ 变化（元素变了） | ❌ 错位 | ✅ **复现！** |

---

## 六、优化建议

### 6.1 立即修复

1. **检查库存扣减返回值**：
   - 文件：`LotteryService.java`
   - 方法：`draw()`
   - 修改：调用 `PrizeRepository.decrementStock()` 后检查返回值，返回0时抛出 `BusinessException`

2. **抽奖前刷新奖品列表**：
   - 文件：`Home.vue`
   - 方法：`handleSpin()`
   - 修改：点击抽奖时，先调用 `getEnabledPrizes()` 刷新前端奖品列表，确保与后端一致

### 6.2 架构优化

1. **使用 prizeId 替代 prizeIndex**：
   - 后端返回 `prizeId` 而非 `prizeIndex`
   - 前端根据 `prizeId` 在本地列表中查找索引
   - 找不到时降级处理（直接弹消息）

2. **WebSocket 实时同步**：
   - 管理员修改奖品时，通过 WebSocket 通知前端刷新
   - 或使用版本号机制

3. **库存预警**：
   - 库存 <= 阈值时，前端自动灰显转盘扇区
   - 抽奖前预检查库存状态

