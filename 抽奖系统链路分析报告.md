# 抽奖系统完整链路分析报告

## 一、整体链路概览

```
管理员操作 → 数据库更新 → 前端加载奖品 → 用户点击抽奖 → 后端抽奖算法 → 结果返回 → 前端动画展示
```

### 1.1 数据流向图

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  管理员后台      │────▶│  数据库(Prize)  │────▶│  前端首页(Home) │
│  PrizeManager   │     │  概率/库存/状态  │     │  LotteryWheel   │
└─────────────────┘     └─────────────────┘     └────────┬────────┘
                                                         │
                                                         ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  结果弹窗展示    │◀────│  后端抽奖服务    │◀────│  用户点击抽奖    │
│  showResult      │     │  LotteryService │     │  handleSpin     │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

---

## 二、各环节详细分析

### 2.1 管理员设置奖品概率和库存

**前端**：
- 文件：`frontend/src/views/admin/PrizeManager.vue`
- 关键方法：
  - `handleSubmit()` - 提交新增/编辑
  - `handleToggle()` - 切换启用状态
  - `handleDelete()` - 删除奖品

**后端**：
- Controller：`backend/src/main/java/com/lottery/controller/PrizeController.java`
  - `createPrize()` - 创建奖品 (POST /api/prizes)
  - `updatePrize()` - 更新奖品 (PUT /api/prizes/{id})
  - `toggleEnabled()` - 切换状态 (PATCH /api/prizes/{id}/toggle)

- Service：`backend/src/main/java/com/lottery/service/PrizeService.java`
  - `createPrize()` - 创建奖品实体并保存
  - `updatePrize()` - 更新奖品字段
  - `toggleEnabled()` - 切换 enabled 字段

- Repository：`backend/src/main/java/com/lottery/repository/PrizeRepository.java`
  - 继承 JpaRepository，通过 save() 方法持久化到数据库

**数据流向**：
```
管理员表单 → PrizeDTO → Prize实体 → PrizeRepository.save() → t_prize表
```

### 2.2 前端加载奖品列表

**前端**：
- 文件：`frontend/src/views/Home.vue`
- 方法：`loadPrizes()`
- 调用 API：`getEnabledPrizes()` → GET /api/prizes/enabled

**后端**：
- Controller：`PrizeController.getEnabledPrizes()`
- Service：`PrizeService.getEnabledPrizes()`
- Repository：`PrizeRepository.findByEnabledTrueOrderBySortOrderAsc()`

**关键点**：
- 前端加载的是 **所有 enabled=true 的奖品**，不考虑库存
- 前端将这些奖品渲染到转盘 `LotteryWheel.vue` 中
- 前端 `onMounted` 时加载一次，之后**不会主动刷新**，除非用户刷新页面

### 2.3 用户触发抽奖

**前端**：
- 文件：`frontend/src/views/Home.vue`
- 方法：`handleSpin()`
- 调用 API：`drawLottery(userId)` → POST /api/lottery/draw/{userId}

**流程**：
1. 检查用户是否登录
2. 检查剩余抽奖次数
3. 设置 `isSpinning = true`
4. 调用后端抽奖接口
5. 接收结果，调用 `spinToIndex(prizeIndex)` 播放动画
6. 显示结果弹窗

### 2.4 后端执行抽奖算法

**后端**：
- Controller：`LotteryController.draw()`
- Service：`LotteryService.draw()` (核心逻辑)

**抽奖流程**（LotteryService.java:44-124）：

```java
1. 验证用户状态和剩余次数
2. 获取前端展示用的奖品列表：
   List<Prize> displayPrizes = prizeRepository.findByEnabledTrueOrderBySortOrderAsc();
   ← 与前端加载的列表完全一致（只看enabled，不看stock）

3. 筛选出有库存的奖品用于抽奖：
   List<Prize> availablePrizes = displayPrizes.stream()
           .filter(p -> p.getStock() > 0)
           .collect(Collectors.toList());
   ← 这里才会过滤掉库存为0的奖品

4. 执行抽奖算法：drawPrize(availablePrizes)
   - 计算总概率
   - 生成随机数
   - 累加概率找到中奖奖品

5. 扣减用户抽奖次数
6. 扣减奖品库存：prizeRepository.decrementStock(prizeId)
7. 保存抽奖记录
8. 计算奖品在前端展示列表中的索引：
   int prizeIndex = displayPrizes.indexOf(prize);
   ← 关键：使用displayPrizes列表计算索引，确保与前端转盘一致
```

### 2.5 前端展示结果

**前端**：
- 文件：`frontend/src/views/Home.vue`
- 方法：`showResult()`

**流程**：
1. 接收后端返回的 `DrawResultDTO`，包含：
   - `prizeIndex` - 奖品在前端列表中的索引
   - `prizeName` - 奖品名称
   - `win` - 是否中奖
   - `remainingChances` - 剩余次数

2. 调用 `LotteryWheel.spinToIndex(prizeIndex, callback)`
   - 计算旋转角度
   - CSS transition 动画旋转 5 秒
   - 动画结束后回调

3. 显示中奖弹窗 `showResultDialog`

---

## 三、关键问题场景分析

### 3.1 场景一：奖品库存归零

**触发条件**：
- 奖品A库存=1，前端已加载显示该奖品
- 用户甲抽走最后1个，库存变为0
- 用户乙的页面没有刷新，转盘上仍显示奖品A

**数据流程**：

| 时间点 | 前端状态 | 后端状态 | 操作 |
|--------|----------|----------|------|
| T0 | 转盘显示[奖品A, 奖品B, 奖品C] | 奖品A:stock=1, enabled=true | 初始状态 |
| T1 | 转盘显示[奖品A, 奖品B, 奖品C] | 奖品A:stock=0, enabled=true | 用户甲抽中奖品A |
| T2 | 转盘显示[奖品A, 奖品B, 奖品C] **(不一致)** | 奖品A:stock=0, enabled=true | 用户乙页面未刷新 |
| T3 | 用户乙点击抽奖 | 后端执行draw() | 抽奖请求 |
| T4 | - | displayPrizes=[A, B, C] (enabled=true) | 后端获取展示列表 |
| T5 | - | availablePrizes=[B, C] (stock>0) | 后端过滤无库存奖品 |
| T6 | - | 从[B, C]中抽奖，假设抽中B | 执行抽奖算法 |
| T7 | - | prizeIndex = displayPrizes.indexOf(B) = 1 | 计算索引 |
| T8 | 转盘旋转到索引1（奖品B） | - | 前端动画 |

**状态分叉分析**：

```
前端状态（T2）:
  转盘扇区：[奖品A, 奖品B, 奖品C]
  索引：     0        1        2

后端抽奖时状态（T4-T5）:
  displayPrizes: [奖品A, 奖品B, 奖品C]  ← 与前端一致
  availablePrizes: [奖品B, 奖品C]        ← 过滤掉A
  
抽奖结果：奖品B
prizeIndex: 1 （在displayPrizes中的位置）

前端展示：转盘旋转到索引1 → 正确指向奖品B ✓
```

**结论**：
- ✅ **不会出现用户看到指针指向A但实际抽中B的情况**
- 原因：后端使用 `displayPrizes` 列表计算 `prizeIndex`，与前端转盘的索引完全对应
- 但前端显示奖品A已"已抽完"状态不及时，需要刷新页面

---

### 3.2 场景二：并发请求同时抽奖

**触发条件**：
- 奖品A库存=1
- 用户甲和用户乙同时（几乎同一毫秒）发起抽奖请求

**数据流程**：

| 时间点 | 用户甲请求 | 用户乙请求 | 数据库状态 |
|--------|-----------|-----------|------------|
| T0 | - | - | 奖品A:stock=1 |
| T1 | 进入draw() | 进入draw() | 奖品A:stock=1 |
| T2 | 读取displayPrizes | 读取displayPrizes | 奖品A:stock=1 |
| T3 | 读取availablePrizes=[A, B, C] | 读取availablePrizes=[A, B, C] | 奖品A:stock=1 |
| T4 | 抽中奖品A | 抽中奖品A | 奖品A:stock=1 |
| T5 | 执行decrementStock(A) | 等待锁 | 奖品A:stock=1 |
| T6 | stock变为0，返回成功 | 执行decrementStock(A) | 奖品A:stock=0 |
| T7 | 返回结果给甲 | WHERE stock>0 不匹配，返回0 | 奖品A:stock=0 |
| T8 | 甲显示中奖A | 抛出异常"抽奖次数扣减失败"？ | 奖品A:stock=0 |

**代码分析**：

`LotteryService.draw()` 方法有 `@Transactional` 注解，但是：

1. **读取库存是在内存中过滤**：
   ```java
   // 第63-66行：从内存中过滤，不是从数据库加锁查询
   List<Prize> availablePrizes = displayPrizes.stream()
           .filter(p -> p.getStock() > 0)
           .collect(Collectors.toList());
   ```

2. **扣减库存使用乐观锁机制**：
   ```java
   // PrizeRepository.java:33-34
   @Query("UPDATE Prize p SET p.stock = p.stock - 1 WHERE p.id = :prizeId AND p.stock > 0")
   int decrementStock(@Param("prizeId") Long prizeId);
   ```
   - 使用 `AND p.stock > 0` 条件，保证不会扣减成负数
   - 返回值 `int` 表示实际更新的行数

3. **但代码有缺陷**：
   ```java
   // LotteryService.java:82-84
   if (prize.getStock() > 0) {
       prizeRepository.decrementStock(prize.getId());
   }
   ```
   - 这里没有检查 `decrementStock()` 的返回值
   - 如果返回0（表示库存已为0），代码不会抛出异常
   - 但用户抽奖次数已经被扣减了（第76行）！

**状态分叉分析**：

```
用户甲：
  availablePrizes读取时：stock=1 ✓
  抽中A
  decrementStock(A) → 返回1，stock变为0 ✓
  用户次数-1 ✓
  结果：成功抽中A ✓

用户乙：
  availablePrizes读取时：stock=1 ✓（与甲同时读取）
  抽中A
  decrementStock(A) → 返回0（因为甲已经改成0）
  用户次数-1 ✓（但代码没有检查decrementStock返回值！）
  结果：
    - 抽奖次数被扣减了 ✗
    - 但没有抽到奖品 ✗
    - 而且不会抛异常！✗
```

**问题总结**：
1. ✗ **超卖风险**：两个并发请求可能都认为奖品A有库存
2. ✗ **用户体验问题**：乙的抽奖次数被扣减但没有获得奖品
3. ✗ **代码缺陷**：没有检查 `decrementStock()` 的返回值

---

### 3.3 场景三：管理员中途改概率

**触发条件**：
- 用户甲的页面已加载，转盘显示奖品A（概率10%）、B（概率20%）、C（70%）
- 管理员在后台将奖品A的概率改为50%
- 用户甲点击抽奖（页面未刷新）

**数据流程**：

| 时间点 | 前端状态 | 后端数据库 | 操作 |
|--------|----------|------------|------|
| T0 | 转盘显示A(10%), B(20%), C(70%) | A:probability=10 | 初始状态 |
| T1 | 转盘显示A(10%), B(20%), C(70%) | A:probability=50 | 管理员修改概率 |
| T2 | 用户甲点击抽奖 | - | 发起抽奖请求 |
| T3 | - | displayPrizes读取概率：A=50, B=20, C=70 | 后端从DB读取 |
| T4 | - | 抽奖算法使用最新概率 | 执行抽奖 |
| T5 | 转盘动画（索引不变） | - | 返回prizeIndex |

**状态分叉分析**：

```
前端感知的概率（未刷新）:
  A: 10%, B: 20%, C: 70%

后端实际使用的概率:
  A: 50%, B: 20%, C: 70%

奖品索引映射（不变）:
  索引0 → A, 索引1 → B, 索引2 → C

抽奖结果：
  - 后端用50%概率抽A，用户更容易中A
  - 但前端不知道概率已改变
  - 指针旋转到正确的索引 ✓
```

**结论**：
- ✅ **不会出现索引错位**（因为奖品数量和顺序没变）
- ⚠️ **概率不一致**：用户以为概率还是10%，但实际是50%
- ✅ **最终结果展示正确**：不管概率多少，抽中哪个就指向哪个

---

## 四、前端奖品渲染列表 vs 后端抽奖可用列表

### 4.1 数据来源对比

| 维度 | 前端渲染列表 | 后端抽奖可用列表 |
|------|-------------|-----------------|
| 数据来源 | `PrizeService.getEnabledPrizes()` | `LotteryService.draw()` 内部 |
| 查询条件 | `enabled = true` | `enabled = true AND stock > 0` |
| 刷新时机 | 页面加载时一次 | 每次抽奖请求时 |
| 是否包含库存为0的奖品 | ✅ 是 | ❌ 否 |
| 是否使用最新概率 | ❌ 使用加载时的 | ✅ 使用最新的 |

### 4.2 差异影响分析

**差异1：库存为0的奖品**

```
前端列表：[A(stock=0), B, C]  ← 显示A，但显示"已抽完"
后端列表：[B, C]              ← 不会抽到A

影响：
  ✓ 不会抽到A（因为后端过滤了）
  ✓ prizeIndex计算正确（用displayPrizes）
  ⚠️ 前端显示A在转盘上，但实际抽不到
```

**差异2：概率修改**

```
前端概率：A=10%, B=20%, C=70%  ← 加载时的概率
后端概率：A=50%, B=20%, C=70%  ← 最新概率

影响：
  ⚠️ 用户感知的概率与实际不一致
  ✓ 索引映射不变，展示正确
```

**差异3：奖品新增/删除/顺序变化**

```
管理员新增奖品D，插入到第一位：
  前端列表：[A, B, C]        ← 未刷新
  后端列表：[D, A, B, C]     ← 最新

影响：
  ✗ 严重问题！prizeIndex会错位！
  后端抽中D → prizeIndex=0
  前端旋转到索引0 → 指向A（而不是D）！
```

---

## 五、问题总结与建议

### 5.1 现有问题

1. **并发超卖风险**：库存读取无锁，可能多个请求同时认为有库存
2. **代码缺陷**：`decrementStock()` 返回值未检查，可能扣减次数但没奖品
3. **前端数据不实时**：奖品列表、库存、概率变化不会自动刷新
4. **奖品列表变化风险**：新增/删除/重排奖品会导致prizeIndex错位

### 5.2 优化建议

**建议1：检查decrementStock返回值**

```java
// LotteryService.java:82-84
int updated = prizeRepository.decrementStock(prize.getId());
if (updated == 0) {
    // 回滚用户抽奖次数
    userRepository.incrementChances(userId);
    throw new BusinessException("奖品库存不足，请刷新页面重试");
}
```

**建议2：使用乐观锁或悲观锁查询库存**

```java
// 在查询时加锁
@Query("SELECT p FROM Prize p WHERE p.enabled = true AND p.stock > 0 ORDER BY p.sortOrder ASC")
@Lock(LockModeType.PESSIMISTIC_WRITE)
List<Prize> findAvailablePrizesForUpdate();
```

**建议3：前端实时刷新机制**

```javascript
// Home.vue - 抽奖前刷新奖品列表
const handleSpin = async () => {
    await loadPrizes();  // 抽奖前刷新
    // ... 继续抽奖
}
```

**建议4：使用prizeId而非prizeIndex**

```javascript
// 后端返回prizeId，前端自己计算索引
// LotteryWheel.vue
const spinToPrizeId = (prizeId, callback) => {
    const index = this.prizes.findIndex(p => p.id === prizeId);
    if (index === -1) {
        // 奖品不存在，刷新列表
        return;
    }
    this.spinToIndex(index, callback);
}
```

**建议5：WebSocket实时推送**

- 管理员修改奖品后，通过WebSocket通知所有前端刷新

---

## 六、核心代码索引

| 功能模块 | 文件路径 | 关键方法 |
|---------|---------|---------|
| 管理员奖品管理 | `frontend/src/views/admin/PrizeManager.vue` | `handleSubmit`, `handleToggle`, `handleDelete` |
| 前端抽奖页面 | `frontend/src/views/Home.vue` | `loadPrizes`, `handleSpin`, `showResult` |
| 转盘组件 | `frontend/src/components/LotteryWheel.vue` | `drawWheel`, `spinToIndex` |
| 奖品Controller | `backend/.../controller/PrizeController.java` | `getEnabledPrizes`, `createPrize`, `updatePrize` |
| 抽奖Controller | `backend/.../controller/LotteryController.java` | `draw` |
| 奖品Service | `backend/.../service/PrizeService.java` | `getEnabledPrizes`, `createPrize`, `updatePrize` |
| 抽奖Service | `backend/.../service/LotteryService.java` | `draw`, `drawPrize` |
| 奖品Repository | `backend/.../repository/PrizeRepository.java` | `findByEnabledTrueOrderBySortOrderAsc`, `decrementStock` |
| 奖品实体 | `backend/.../entity/Prize.java` | `hasStock()` |

---

**报告生成时间**：2026-05-10
